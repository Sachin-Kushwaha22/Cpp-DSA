#include <bits/stdc++.h>
using namespace std;

int selectionSortRec(vector<int> &arr, int size)
{
    int start=0;
    int end=size-1;
    for (int i = 0; i < size; i++)
    {
        for (int j = i+1; j < size; j++)
        {
            if(arr[i])
        }
        
    }
    
    
}

int main()
{
    vecotr<int> vec = {3, 6, 8, 1, 2, 9};
    int size = vec.size();
    selectionSortRec(vec, size);
    return 0;
}